import React from 'react';
import styled from 'styled-components';

const Sidebar = () => {
    return <Wrapper>h2 sidebar</Wrapper>;
};

export default Sidebar;

const Wrapper = styled.aside`
    background-color: red;
    flex: 1;
`;
